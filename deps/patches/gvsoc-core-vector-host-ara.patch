diff --git a/models/interco/router/router.cpp b/models/interco/router/router.cpp
index 9397be3..883a45d 100644
--- a/models/interco/router/router.cpp
+++ b/models/interco/router/router.cpp
@@ -552,7 +552,13 @@ void BandwidthLimiter::apply_bandwidth(i
 {
     uint64_t size = req->get_size();
 
-    if (this->bandwidth != 0)
+    // A debug access -- the loader, a debugger, semi-hosting, or a timing-only
+    // cache fetching the bytes of a hit from the level below -- is not a
+    // transfer on this bus. It must not occupy the bandwidth real requests are
+    // queued against: charging it moved the burst cyclestamp ahead of
+    // simulated time with every such access, and every real request behind it
+    // then waited for traffic that never crossed the router.
+    if (this->bandwidth != 0 && !req->is_debug())
     {
         // Bandwidth was specified
 
@@ -585,7 +591,8 @@ void BandwidthLimiter::apply_bandwidth(i
     }
     else
     {
-        // No bandwidth was specified, just add the specified latency
+        // No bandwidth was specified, or this is a debug access, which crosses no
+        // bus: just add the specified latency
         req->inc_latency(this->latency);
     }
 }
diff --git a/models/cpu/iss/isa_gen/isa_rvv_timed.py b/models/cpu/iss/isa_gen/isa_rvv_timed.py
index 01f3b25..ec440ee 100644
--- a/models/cpu/iss/isa_gen/isa_rvv_timed.py
+++ b/models/cpu/iss/isa_gen/isa_rvv_timed.py
@@ -276,6 +276,11 @@ Format_OPVLS = [ OutVReg     (0, Range(7
                  InVReg      (0, Range(15, 5)),
                  UnsignedImm(0, Range(25, 0)),
 ]
+# vid.v: a destination vector register and the mask bit, and no operand --
+# each active element is written with its own index.
+Format_OPV_VID = [ OutVReg     (0, Range(7 , 5)),
+                   UnsignedImm (0, Range(25, 1)),
+]
 #                           V 0.8
 # Format_OPVLI':
 #     self.args = [ OutReg     (0, Range(7 , 5)),
@@ -300,6 +305,9 @@ class Rv32v(IsaSubset):
 
     def __init__(self):
         super().__init__(name='v', instrs=[
+            # vid.v writes each active element's index. GCC emits it for the
+            # index vectors of gathers, e.g. an autovectorized matmul.
+            Instr('vid.v'         ,   Format_OPV_VID ,    '010100 - 00000 10001 010 ----- 1010111'),
             Instr('vadd.vv'       ,   V_VVU , '000000 - ----- ----- 000 ----- 1010111'),
             Instr('vadd.vi'       ,   V_VIU , '000000 - ----- ----- 011 ----- 1010111'),
             Instr('vadd.vx'       ,   V_VXU , '000000 - ----- ----- 100 ----- 1010111'),
diff --git a/models/cpu/iss/include/isa/rv32v_timed.hpp b/models/cpu/iss/include/isa/rv32v_timed.hpp
index b1db49a..c4d1b10 100644
--- a/models/cpu/iss/include/isa/rv32v_timed.hpp
+++ b/models/cpu/iss/include/isa/rv32v_timed.hpp
@@ -949,7 +949,7 @@ static inline iss_reg_t vmv_v_v_exec(Iss
 static inline iss_reg_t vmv_v_x_exec(Iss *iss, iss_insn_t *insn, iss_reg_t pc)
 {
     unsigned int sewb = iss->vector.sewb;
-    uint64_t in = convert_scalar_to_vector(REG_GET(0), sewb * 8);
+    uint64_t in = convert_scalar_to_vector(RVV_REG_GET(0), sewb * 8);
     for (unsigned int i=VSTART; i<VEND; i++)
     {
         if (velem_is_active(iss, i, UIM_GET(0)))
@@ -974,10 +974,25 @@ static inline iss_reg_t vmv_v_i_exec(Iss
     return iss_insn_next(iss, insn, pc);
 }
 
+// vid.v: each active element takes its own index, truncated to SEW like any
+// other integer write.
+static inline iss_reg_t vid_v_exec(Iss *iss, iss_insn_t *insn, iss_reg_t pc)
+{
+    unsigned int sewb = iss->vector.sewb;
+    for (unsigned int i=VSTART; i<VEND; i++)
+    {
+        if (velem_is_active(iss, i, UIM_GET(0)))
+        {
+            velem_set_value(iss, REG_OUT(0), i, sewb, i);
+        }
+    }
+    return iss_insn_next(iss, insn, pc);
+}
+
 static inline iss_reg_t vmv_s_x_exec(Iss *iss, iss_insn_t *insn, iss_reg_t pc)
 {
     unsigned int sewb = iss->vector.sewb;
-    uint64_t in = convert_scalar_to_vector(REG_GET(0), sewb * 8);
+    uint64_t in = convert_scalar_to_vector(RVV_REG_GET(0), sewb * 8);
     velem_set_value(iss, REG_OUT(0), 0, sewb, in);
     return iss_insn_next(iss, insn, pc);
 }
@@ -3821,7 +3836,7 @@ static inline iss_reg_t vfmv_v_f_exec(Is
 {
     unsigned int sewb = iss->vector.sewb;
 
-    uint64_t value = FREG_GET(0);
+    uint64_t value = RVV_FREG_GET(0);
 
     for (unsigned int i=VSTART; i<VEND; i++)
     {
@@ -3839,7 +3854,7 @@ static inline iss_reg_t vfmv_s_f_exec(Is
     unsigned int sewb = iss->vector.sewb;
     unsigned int lmul = iss->vector.lmul;
 
-    velem_set_value(iss, REG_OUT(0), 0, sewb, FREG_GET(0));
+    velem_set_value(iss, REG_OUT(0), 0, sewb, RVV_FREG_GET(0));
 
     return iss_insn_next(iss, insn, pc);
 }
@@ -3879,8 +3894,13 @@ static inline iss_reg_t vsetvl_handle(Is
     iss->vector.sewb = 1 << sew;
     extract_format(iss->vector.sewb * 8, &iss->vector.mant, &iss->vector.exp);
 
-    // Only update VL if an output register is specified (required by specs)
-    if (idxRd)
+    // vl is left alone only by the vtype-only form, where rd and rs1 are both
+    // x0; every other form sets it. A write to x0 is decoded to a discard
+    // register at or past ISS_NB_REGS rather than to 0, so rd cannot be tested
+    // against 0 alone: doing so reset vl to VLMAX on every vsetvli zero, zero,
+    // the form a compiler uses to change SEW or LMUL mid-loop.
+    bool rd_is_x0 = idxRd == 0 || idxRd >= ISS_NB_REGS;
+    if (!(rd_is_x0 && idxRs1 == 0))
     {
         int vlmax = vlmax_get(iss);
 
@@ -4131,7 +4151,7 @@ static inline iss_reg_t vwadd_vx_exec(Is
 {
     unsigned int sewb = iss->vector.sewb;
     unsigned int lmul = iss->vector.lmul;
-    int64_t in0 = get_signed_value(REG_GET(0), sewb * 8);
+    int64_t in0 = get_signed_value(RVV_REG_GET(0), sewb * 8);
     for (unsigned int i=VSTART; i<VEND; i++)
     {
         if (velem_is_active(iss, i, UIM_GET(0)))
@@ -4165,7 +4185,7 @@ static inline iss_reg_t vwsub_vx_exec(Is
 {
     unsigned int sewb = iss->vector.sewb;
     unsigned int lmul = iss->vector.lmul;
-    int64_t in0 = get_signed_value(REG_GET(0), sewb * 8);
+    int64_t in0 = get_signed_value(RVV_REG_GET(0), sewb * 8);
     for (unsigned int i=VSTART; i<VEND; i++)
     {
         if (velem_is_active(iss, i, UIM_GET(0)))
@@ -4199,7 +4219,7 @@ static inline iss_reg_t vwsubu_vx_exec(I
 {
     unsigned int sewb = iss->vector.sewb;
     unsigned int lmul = iss->vector.lmul;
-    uint64_t in0 = REG_GET(0);
+    uint64_t in0 = RVV_REG_GET(0);
     for (unsigned int i=VSTART; i<VEND; i++)
     {
         if (velem_is_active(iss, i, UIM_GET(0)))
@@ -4234,7 +4254,7 @@ static inline iss_reg_t vwaddu_vx_exec(I
 {
     unsigned int sewb = iss->vector.sewb;
     unsigned int lmul = iss->vector.lmul;
-    uint64_t in0 = REG_GET(0);
+    uint64_t in0 = RVV_REG_GET(0);
     for (unsigned int i=VSTART; i<VEND; i++)
     {
         if (velem_is_active(iss, i, UIM_GET(0)))
diff --git a/models/cpu/iss_v2/include/cores/vector_unit/vector_unit.hpp b/models/cpu/iss_v2/include/cores/vector_unit/vector_unit.hpp
index 17b88d2..6f53f90 100644
--- a/models/cpu/iss_v2/include/cores/vector_unit/vector_unit.hpp
+++ b/models/cpu/iss_v2/include/cores/vector_unit/vector_unit.hpp
@@ -513,6 +513,15 @@ private:
     // Handler for asynchronous burst responses
     static void data_response(vp::Block *__this, vp::IoReq *req);
     void handle_burst_end(vp::IoReq *req);
+    // Strided (vlse/vsse) and indexed (vlux/vsux) accesses name one address per
+    // element, so they are issued an element per burst instead of as one
+    // contiguous range from the base address.
+    static void handle_insn_load_strided(VuLsu *vlsu, iss_insn_t *insn);
+    static void handle_insn_store_strided(VuLsu *vlsu, iss_insn_t *insn);
+    static void handle_insn_load_indexed(VuLsu *vlsu, iss_insn_t *insn);
+    static void handle_insn_store_indexed(VuLsu *vlsu, iss_insn_t *insn);
+    void handle_access(iss_insn_t *insn, bool is_write, int reg, bool do_stride=false,
+        iss_reg_t stride=0, int reg_indexed=-1);
 
     // Number of bursts which can be sent at the same time
     static constexpr int nb_burst = 8;
@@ -552,6 +561,14 @@ private:
     std::queue<vp::IoReq *> free_bursts;
     // Memory interface for load/store bursts
     vp::IoMaster io_itf;
+    // Stride of the current access in bytes, and whether it is a strided one
+    iss_reg_t stride;
+    bool strided;
+    // Vector register holding the offsets of an indexed access, or -1
+    int reg_indexed;
+    // Next element of an indexed access, and the width of its offsets
+    int pending_elem;
+    int inst_elem_size;
     // True if one burst was not granted. Once it is true, the block can not send any burst
     // anymore until the last one is granted
     bool stalled;
diff --git a/models/cpu/iss_v2/src/cores/ara/ara_vlsu.cpp b/models/cpu/iss_v2/src/cores/ara/ara_vlsu.cpp
index 44e364c..1a57ce4 100644
--- a/models/cpu/iss_v2/src/cores/ara/ara_vlsu.cpp
+++ b/models/cpu/iss_v2/src/cores/ara/ara_vlsu.cpp
@@ -75,6 +75,24 @@ void VuLsu::isa_init()
     {
         insn->u.insn.block_handler = (void *)&VuLsu::handle_insn_store;
     }
+    // Strided and indexed accesses carry the vload/vstore tag as well, so they
+    // are bound last to take precedence.
+    for (iss_decoder_item_t *insn: *this->vu.iss.decode.get_insns_from_tag("vload_strided"))
+    {
+        insn->u.insn.block_handler = (void *)&VuLsu::handle_insn_load_strided;
+    }
+    for (iss_decoder_item_t *insn: *this->vu.iss.decode.get_insns_from_tag("vstore_strided"))
+    {
+        insn->u.insn.block_handler = (void *)&VuLsu::handle_insn_store_strided;
+    }
+    for (iss_decoder_item_t *insn: *this->vu.iss.decode.get_insns_from_tag("vload_indexed"))
+    {
+        insn->u.insn.block_handler = (void *)&VuLsu::handle_insn_load_indexed;
+    }
+    for (iss_decoder_item_t *insn: *this->vu.iss.decode.get_insns_from_tag("vstore_indexed"))
+    {
+        insn->u.insn.block_handler = (void *)&VuLsu::handle_insn_store_indexed;
+    }
 }
 
 void VuLsu::enqueue_insn(PendingInsn *pending_insn)
@@ -100,38 +118,66 @@ void VuLsu::enqueue_insn(PendingInsn *pe
 }
 
 
-void VuLsu::handle_insn_load(VuLsu *_this, iss_insn_t *insn)
+void VuLsu::handle_access(iss_insn_t *insn, bool is_write, int reg, bool do_stride,
+    iss_reg_t stride, int reg_indexed)
 {
-    // A load instruction is starting, just store information about the first burst and let
-    // the FSM handle all the bursts.
-    VuLsuPendingInsn &slot = _this->insns[_this->insn_first_waiting];
-    unsigned int sewb = _this->vu.iss.vector.sewb;
-    unsigned int lmul = _this->vu.iss.vector.lmul;
-    _this->pending_velem = velem_get(&_this->vu.iss, insn->out_regs[0], 0, sewb, lmul);
-    _this->pending_addr = slot.insn->reg;
-    _this->pending_is_write = false;
-    int elem_size = insn->uim[1] >= 5 ? 1 << (insn->uim[1] - 4) : 1 << 0;
-    _this->elem_size = elem_size;
-    _this->pending_size = (_this->vu.iss.csr.vl.value - _this->vu.iss.csr.vstart.value) * elem_size;
-    _this->vstart = _this->vu.iss.csr.vstart.value;
+    // A load or store instruction is starting, just store information about the first burst and
+    // let the FSM handle all the bursts.
+    VuLsuPendingInsn &slot = this->insns[this->insn_first_waiting];
+    unsigned int sewb = this->vu.iss.vector.sewb;
+    unsigned int lmul = this->vu.iss.vector.lmul;
+    this->pending_velem = velem_get(&this->vu.iss, reg, 0, sewb, lmul);
+    this->pending_addr = slot.insn->reg;
+    this->pending_is_write = is_write;
+    // The width field of the opcode is the element width of a unit-stride or
+    // strided access, but the width of the offsets of an indexed one, whose
+    // elements are SEW wide.
+    int inst_elem_size = insn->uim[1] >= 5 ? 1 << (insn->uim[1] - 4) : 1 << 0;
+    int elem_size = reg_indexed != -1 ? sewb : inst_elem_size;
+    this->inst_elem_size = inst_elem_size;
+    this->elem_size = elem_size;
+    this->pending_size = (this->vu.iss.csr.vl.value - this->vu.iss.csr.vstart.value) * elem_size;
+    this->vstart = this->vu.iss.csr.vstart.value;
+    this->stride = stride;
+    this->strided = do_stride;
+    this->reg_indexed = reg_indexed;
+    this->pending_elem = 0;
     slot.done = 0;
 }
 
+void VuLsu::handle_insn_load(VuLsu *_this, iss_insn_t *insn)
+{
+    _this->handle_access(insn, false, insn->out_regs[0]);
+}
+
 void VuLsu::handle_insn_store(VuLsu *_this, iss_insn_t *insn)
 {
-    // A store instruction is starting, just store information about the first burst and let
-    // the FSM handle all the bursts.
-    VuLsuPendingInsn &slot = _this->insns[_this->insn_first_waiting];
-    unsigned int sewb = _this->vu.iss.vector.sewb;
-    unsigned int lmul = _this->vu.iss.vector.lmul;
-    _this->pending_velem = velem_get(&_this->vu.iss, insn->in_regs[1], 0, sewb, lmul);
-    _this->pending_addr = slot.insn->reg;
-    _this->pending_is_write = true;
-    int elem_size = insn->uim[1] >= 5 ? 1 << (insn->uim[1] - 4) : 1 << 0;
-    _this->elem_size = elem_size;
-    _this->pending_size = (_this->vu.iss.csr.vl.value - _this->vu.iss.csr.vstart.value) * elem_size;
-    _this->vstart = _this->vu.iss.csr.vstart.value;
-    slot.done = 0;
+    _this->handle_access(insn, true, insn->in_regs[1]);
+}
+
+// Same register conventions as the Spatz VLSU, which decodes the same ISA: the
+// stride is the second integer operand of a load and the third of a store, and
+// the offsets are in the vector register after the data one.
+void VuLsu::handle_insn_load_strided(VuLsu *_this, iss_insn_t *insn)
+{
+    _this->handle_access(insn, false, insn->out_regs[0], true,
+        _this->insns[_this->insn_first_waiting].insn->reg_2);
+}
+
+void VuLsu::handle_insn_store_strided(VuLsu *_this, iss_insn_t *insn)
+{
+    _this->handle_access(insn, true, insn->in_regs[1], true,
+        _this->insns[_this->insn_first_waiting].insn->reg_3);
+}
+
+void VuLsu::handle_insn_load_indexed(VuLsu *_this, iss_insn_t *insn)
+{
+    _this->handle_access(insn, false, insn->out_regs[0], false, 0, insn->in_regs[1]);
+}
+
+void VuLsu::handle_insn_store_indexed(VuLsu *_this, iss_insn_t *insn)
+{
+    _this->handle_access(insn, true, insn->in_regs[1], false, 0, insn->in_regs[2]);
 }
 
 void VuLsu::data_grant(vp::Block *__this, vp::IoReq *req)
@@ -217,7 +263,9 @@ void VuLsu::fsm_handler(vp::Block *__thi
 
         if (_this->vu.insn_ready(pending_insn))
         {
-            uint64_t size = std::min((iss_addr_t)_this->vu.nb_lanes * _this->vu.lane_width / 2, _this->pending_size);
+            // A strided or indexed access names an address per element
+            uint64_t size = _this->strided || _this->reg_indexed != -1 ? _this->elem_size :
+                std::min((iss_addr_t)_this->vu.nb_lanes * _this->vu.lane_width / 2, _this->pending_size);
             _this->trace.msg(vp::Trace::LEVEL_TRACE,
                 "Sending request (addr: 0x%lx, pending_size: 0x%lx, is_write: %d)\n",
                 _this->pending_addr, _this->pending_size, _this->pending_is_write);
@@ -229,7 +277,13 @@ void VuLsu::fsm_handler(vp::Block *__thi
             vp::IoReq *req = _this->free_bursts.front();
             _this->free_bursts.pop();
             req->init();
-            req->set_addr(_this->pending_addr);
+            iss_reg_t addr = _this->pending_addr;
+            if (_this->reg_indexed != -1)
+            {
+                addr += velem_get_value(&_this->vu.iss, _this->reg_indexed, _this->pending_elem,
+                    _this->inst_elem_size, _this->vu.iss.vector.lmul);
+            }
+            req->set_addr(addr);
             req->set_size(size);
             req->set_is_write(_this->pending_is_write);
             req->set_data(_this->pending_velem);
@@ -262,7 +316,14 @@ void VuLsu::fsm_handler(vp::Block *__thi
             }
 
             // Prepare the next burst
-            _this->pending_addr += size;
+            if (_this->reg_indexed == -1)
+            {
+                _this->pending_addr += _this->strided ? _this->stride : size;
+            }
+            else
+            {
+                _this->pending_elem++;
+            }
             _this->pending_size -= size;
             _this->pending_velem += size;
 
